name: ci
on:
  pull_request:
  push:
    branches: ["main"]
jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build (release)
        run: swift build -c release
      - name: Assemble .app
        run: bash Scripts/build-app.sh 0.0.0
